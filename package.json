{
  "name": "kotoba-pwa",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/pwa-app.js --bundle --minify --format=iife --outfile=app.js",
    "test:pwa": "node tests/test-pwa.mjs"
  },
  "dependencies": {
    "ts-fsrs": "^5.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
